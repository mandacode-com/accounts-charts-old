{{- if not .Values.app.token.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "app.token.secretName" . }}
  labels:
    app: {{ .Release.Name }}
type: Opaque
data:
  ACCESS_PRIVATE_KEY: {{ .Values.app.token.access.privateKey | b64enc | quote }}
  REFRESH_PRIVATE_KEY: {{ .Values.app.token.refresh.privateKey | b64enc | quote }}
  EMAIL_VERIFICATION_PRIVATE_KEY: {{ .Values.app.token.emailVerification.privateKey | b64enc | quote }}
{{- end }}
