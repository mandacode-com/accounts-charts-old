{{- if .Values.mailer.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "app.mailer.name" . }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "app.mailer.name" . }}
  minReplicas: {{ .Values.mailer.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.mailer.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.mailer.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.mailer.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.mailer.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.mailer.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
